<div class="container">
    <div class="row">
                <img class="img" src="{{this.image_url}}" alt="beer" />
                <p>{{this.name}}</p>
                <p>{{this.description}}</p>
                 <p>{{this.tagline}}</p>
    </div>
</div>